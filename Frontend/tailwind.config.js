/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // 👈 habilita modo escuro via classe
    content: ["./**/*.php", "./**/*.html"], // 👈 arquivos onde Tailwind vai olhar classes
    theme: {
      extend: {},
    },
    plugins: [],
  }
  